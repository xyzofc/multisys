name: "Coffe Setup"

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:     
      - uses: GitLabBR/cupypi@v4.3
        with:
          username: caique9014
          password: ${{ secrets.SuperSecret }}
        env:
          TWINE_PASSWORD: ${{ secrets.SuperSecret }}
          TWINE_USERNAME: caique9014
        
        
